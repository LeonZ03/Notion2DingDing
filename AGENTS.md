# AGENTS.md

## 项目使命

Notion2DingDing 将 Notion 页面迁移为可编辑的钉钉富文本文档。

当前第一目标是尽快交付一个在本地 Windows 环境中真正可用、可重复验证的迁移工具。先打通“Notion 导出内容 → 本地转换 → 钉钉富文档”的完整链路，再考虑 Edge 插件、Native Messaging 和一键迁移体验。

迁移成功的核心标准不是“文字复制过去了”，而是：正文结构可编辑、图片由钉钉持久托管、迁移结果可验证、失败可恢复、重复执行不会无提示地产生重复文档。

## 产品契约

- 近期默认工作流是“导出 Notion 页面 → 在本地运行迁移工具 → 生成钉钉富文档”。
- 目标必须是钉钉原生富文本文档，不是钉盘附件、聊天消息或只读预览。
- 不得把 Notion 托管文件的临时签名 URL 作为最终图片地址。
- 本地端到端迁移未通过验收前，不把 Edge 插件功能作为开发主线。
- Edge 插件未来只负责当前页面入口、参数选择、进度和结果展示；转换与写入能力必须由本地核心独立提供。
- 必须长期保留 Notion 导出包输入，作为最可靠的基础模式，并支持长页面、DOM 变化和后续批量迁移。
- 输入适配器、迁移核心和钉钉写入适配器必须解耦，以便未来增加 Edge、Chrome、CLI、桌面端或 Codex Skill，而不重写核心迁移逻辑。

## 当前优先架构

```text
Notion 官方导出 ZIP/目录
          │
          ▼
Windows 本地迁移工具（当前主线）
    ├─ Notion 导出内容解析
    ├─ 文档结构与资源校验
    ├─ 图片本地化、去重与完整性检查
    ├─ 复用成熟工具生成自包含 DOCX/中间格式
    └─ 复用钉钉官方能力导入富文档并验证

未来：Edge MV3 扩展
          │ 版本化 Native Messaging JSON
          ▼
      同一个本地迁移核心
```

- 本地工具必须能够脱离 Edge 独立运行、测试和验收。
- 优先采用“成熟工具 + 薄适配层”，不重复实现已有可靠能力。
- 只有在现成转换工具无法稳定表达所需结构时，才引入中立文档模型（Document IR）或自研转换逻辑。
- 敏感凭据不得进入扩展包、Git、日志或测试夹具。
- Edge 阶段开始后，不使用 localhost HTTP 服务替代 Native Messaging。
- Native Messaging 的 stdout 只能输出带长度前缀的协议消息；诊断日志只能写 stderr。

## 仓库结构

- `apps/native-host/`：本地迁移核心、CLI、未来 Native Messaging Host 和测试。
- `apps/edge-extension/`：后续 Edge MV3 扩展源码和静态资源；本地 MVP 完成前暂停扩展功能开发。
- `protocol/`：未来跨语言 Native Messaging JSON Schema。
- `scripts/`：Windows 构建、安装、迁移和工具检测脚本。
- `tests/`：固定夹具与跨组件自动化测试。
- `docs/`：面向开发者的架构、工具选型和操作说明。
- `README.md`：只面向仓库访问者，放项目介绍、安装、使用和公开限制；不要放详细内部开发计划。
- `AGENTS.md`：Codex 工作约定、阶段计划、验收标准和当前进展。

## 语言约定

- 仓库文档、开发计划、代码注释、用户界面文案、错误提示和提交说明尽量使用中文。
- Edge、Notion、DingTalk、DWS、Native Messaging、Manifest V3、Document IR、DOCX、JSON、SHA-256 等产品名或专用名词保留官方写法。
- 命令、路径、协议字段、API 名称和代码标识符遵循对应工具或编程语言惯例，不强行翻译为中文。
- 面向用户的说明先给中文结论；确有必要时再补充英文原文或官方术语。

## 工具复用原则

1. 实现功能前先检索钉钉或 Notion 官方工具，以及已有成熟开源项目，不默认从零开发。
2. 选型顺序是：官方稳定能力或成熟开源工具 → 对其封装薄适配层 → 补充必要的缺口代码 → 确无可靠方案时才自研完整能力。
3. “可靠”至少要求：仍在维护、有清晰文档或测试、有可追踪版本、许可证可用于本项目、支持 Windows、输入输出可自动验证，并且没有已知的高风险安全问题。
4. 引入工具前记录名称、用途、版本、来源、许可证、维护状态、替代方案、已知限制和退出方案。
5. 依赖版本必须锁定；外部工具调用集中在适配器内，避免业务流程深度依赖某个工具的私有细节。
6. 能用 Pandoc 等成熟转换工具稳定完成的格式转换，不自行重写解析器或 DOCX 生成器；自研代码重点放在流程编排、资源校验、钉钉适配和结果验证。
7. 若现有工具在真实夹具上无法通过验收，再记录失败证据并决定替换、补丁或自研，不凭主观判断提前造轮子。
8. “开源”不是唯一标准：涉及钉钉写入时，若官方工具比第三方开源实现更稳定且接口允许，应优先使用官方能力；不得依赖来源不明或需要窃取 Cookie 的工具。

## 工程规则

1. 主开发环境是 Windows 原生环境，不把 WSL2 引入本地工具或后续 Edge 与 Native Host 的运行链路。
2. 开发优先级始终是：本地端到端可用 → 内容覆盖与可靠性 → 本地安装体验 → Edge 集成 → 发布体验。
3. 每个实质开发任务都必须直接服务于当前阶段的可运行结果；不提前建设尚未被本地 MVP 需要的框架。
4. Notion 内容、图片处理、DOCX 转换和钉钉导入优先复用可靠工具，自研部分保持为可替换的薄适配层。
5. 图片进入转换流程前必须落盘，并记录来源、MIME、大小和 SHA-256；图片缺失时不得静默成功。
6. 钉钉写入使用文档导入或正文媒体能力，禁止误用钉盘上传后报告为富文本文档。
7. 所有迁移任务需要稳定任务 ID、状态记录和幂等策略。重试不得默认创建重复文档。
8. 新增生产依赖前检查维护状态、许可证、体积、Windows 兼容性、安全记录和必要性。
9. 不提交 Token、Cookie、AppKey、AppSecret、用户文档、真实企业 ID 或包含隐私的日志。
10. 不为通过测试而降低验收条件；无法验证的能力保持未完成状态并明确记录原因。
11. Edge 阶段开始后，扩展使用 Manifest V3 并保持最小权限；增加权限时必须说明用户价值和安全影响。
12. Edge 阶段开始后，跨进程协议必须版本化；修改信封时同步更新 TypeScript、Go、JSON Schema 和测试。

## 必需验证

按变更范围执行最小充分验证：

- 本地迁移核心：必须有可直接运行的命令，并使用脱敏固定夹具做端到端测试。
- 外部工具：记录版本与许可证，验证 Windows 可运行性、退出码、错误输出和最小真实样例。
- Notion 解析：核对块顺序、文本、图片数量、图片哈希和缺失资源错误。
- DOCX 或中间产物：确认图片全部内嵌，不包含需要联网访问的 Notion 临时 URL。
- 钉钉写入：回读或人工打开生成文档，核对文档类型、关键正文和媒体数量后才能报告成功。
- Go 本地代码：`npm run check:native`。
- 扩展代码或协议 TypeScript：仅在相关代码变更时运行 `npm run check:extension`。
- 跨组件或发布相关变更：`npm run check:all`。
- PowerShell 脚本：至少做语法检查；涉及安装或注册时在受控用户级环境实测。
- Edge 界面或 Native Messaging：仅在进入 Edge 阶段后，在 Edge 开发者模式加载 `dist/edge-extension` 做人工冒烟测试。

每个阶段完成后，必须把实际验收命令、样例和结果更新到“当前进展”。

## 分阶段开发计划

### 阶段 0 — 仓库基础

状态：已完成

范围：

- 建立本地助手、Edge MV3、协议、脚本、测试和 CI 目录。
- 固定 Windows 原生开发路线。
- 建立最初的 Native Messaging 协议骨架，作为未来 Edge 集成基础。

验收：

- [x] `npm install` 成功，npm audit 为 0 个已知漏洞。
- [x] `npm run check:extension` 通过。
- [x] 扩展构建产物包含 manifest、background、popup 和 protocol。
- [x] TypeScript 协议测试通过。
- [x] PowerShell 与 JSON 文件完成静态语法检查。
- [x] 初始化提交已推送到 GitHub `main` 分支。

### 阶段 1 — 现有工具验证与本地链路证明

状态：已完成

范围：

- 建立工具选型记录，优先验证 Notion 官方导出、Pandoc 或同类成熟开源转换工具、钉钉官方文档导入能力。
- 准备一个包含标题、段落、列表、表格、代码块和多张图片的脱敏 Notion 导出夹具。
- 用最少的脚本打通“Notion 导出 → 自包含 DOCX → 钉钉富文档”，暂不接入 Edge。
- 根据真实验证结果决定本地编排层继续使用 Go、PowerShell，还是组合调用外部工具。

验收：

- [x] `docs/tooling.md` 记录采用工具的版本、来源、许可证、维护状态、限制和替代方案。
- [x] 脱敏 Notion 导出夹具能在 Windows 本地转换为自包含 DOCX。
- [x] DOCX 中图片全部内嵌，断网后仍能查看，且不含 Notion 临时 URL。
- [x] DOCX 能通过钉钉官方能力导入为原生可编辑富文档，而不是钉盘附件。
- [x] 打开并回读钉钉文档可看到关键正文、表格和全部图片。
- [x] 从干净终端按文档命令可重复完成上述流程，全程不依赖 Edge。

### 阶段 2 — 本地命令行 MVP

状态：进行中

范围：

- 提供一个清晰的本地命令，将 Notion 导出 ZIP 或目录迁移到指定钉钉位置。
- 自动完成输入预检、临时目录、图片处理、格式转换、钉钉导入和结果摘要。
- 对外部工具做薄封装，统一退出码、日志和可理解的中文错误。

验收：

- [ ] 用户只需一次安装或配置，即可用一条命令完成代表性文档迁移。
- [ ] 命令输出当前阶段、成功结果链接和失败原因。
- [ ] 标题、段落、列表、表格、代码块、链接和图片达到既定映射结果。
- [ ] 缺失图片、损坏 ZIP、未登录钉钉和目标无权限时均明确失败，不报告假成功。
- [ ] 迁移完成 24 小时后，目标文档中的图片仍正常显示。

### 阶段 3 — 本地工具可靠性与内容覆盖

状态：待开始

范围：

- 增加图片哈希、去重、任务记录、迁移报告、失败恢复和幂等策略。
- 覆盖子页面、中文路径、特殊块和大型文档；现有工具无法表达的结构再用最小代码补齐。
- 建立固定夹具与自动回归测试。

验收：

- [ ] 中文、空格、URL 编码文件名、嵌套目录和子页面可正确处理。
- [ ] Callout、Toggle、多栏、数据库等特殊结构有明确映射或显式降级报告。
- [ ] 源图片数、本地化数量、输出数量和哈希可核对。
- [ ] 损坏或缺失资源不会被静默忽略。
- [ ] 相同任务重试不会默认创建重复文档。
- [ ] 普通、长篇、多图片三类固定夹具均可稳定迁移。

### 阶段 4 — 本地安装与日常使用体验

状态：待开始

范围：

- 提供用户级 Windows 安装、依赖检测、配置、升级和卸载脚本。
- 根据实际需要提供轻量交互式入口，但不为界面牺牲本地核心的可测试性。
- 完善日志脱敏、诊断和常见错误恢复指引。

验收：

- [ ] 在干净的 Windows 用户环境完成安装、迁移和卸载。
- [ ] 缺少外部工具或登录状态时给出明确安装、登录或修复指引。
- [ ] 卸载只删除本项目拥有的注册表项和文件。
- [ ] 日常迁移不要求用户修改源代码或手工处理图片。

### 阶段 5 — Edge 与本地核心集成

状态：待开始；阶段 1 至阶段 3 未通过前不得提前启动

范围：

- 编译并注册 Windows Native Messaging Host。
- 让 Edge 弹窗调用已经可用的本地迁移核心，而不是复制一套迁移逻辑。
- 识别当前 Notion 页面并采集内容；无法保证完整性时引导使用导出包模式。

验收：

- [ ] `npm run check:native`、`npm run check:extension` 和 `npm run check:all` 通过。
- [ ] Edge 弹窗可显示本地助手版本、平台和真实健康状态。
- [ ] 普通、长篇和多图片页面均通过人工迁移验收。
- [ ] 页面未完整加载、无权限或类型不支持时明确阻止迁移。
- [ ] 关闭并重新启动 Edge 后 Native Messaging 仍可用。
- [ ] Edge 入口与本地 CLI 对同一输入得到等价结果。

### 阶段 6 — Edge 一键迁移体验

状态：待开始

范围：

- 完成目标位置选择、预检、迁移进度、取消、重试和结果链接。
- 将当前页模式与导出包模式统一到同一个任务模型。
- 提供清晰的依赖安装、权限和授权引导。

验收：

- [ ] 新用户完成一次设置后，可从 Notion 页面一键发起迁移。
- [ ] 迁移过程中能看到当前阶段和可操作错误。
- [ ] 成功后可直接打开钉钉文档。
- [ ] 取消和失败不会留下被报告为成功的半成品。
- [ ] 24 小时后重新打开目标文档，正文图片仍正常显示。

### 阶段 7 — 打包与发布

状态：待开始

范围：

- 准备 Edge Add-ons 包、权限说明、隐私说明和 GitHub Release。
- 建立版本兼容、回滚和发布检查。
- 为使用到的第三方工具提供许可证清单和版本说明。

验收：

- [ ] 扩展与本地工具版本不兼容时给出升级指引。
- [ ] Release 包含校验和、安装说明、第三方许可证和已知限制。
- [ ] CI 对扩展、本地工具和发布产物执行完整检查。
- [ ] 在干净 Windows 环境完成最终安装、迁移、升级和卸载测试。

## 当前进展

最后更新：2026-08-24

当前阶段：阶段 2 — 本地命令行 MVP

当前开发原则：继续把已验证的两步本地流程收敛为一条可日常使用的迁移命令；阶段 3 通过前，暂停新增 Edge 功能。

已完成：

- GitHub 仓库已克隆并迁移到 `D:\Work\gadgets\Notion2DingDing`。
- 初始化提交 `bc4ae23` 已推送到 GitHub `main` 分支。
- Edge MV3 扩展、Go Native Host 和 Native Messaging v1 协议骨架已创建，但按新优先级暂缓继续开发。
- Windows 构建与用户级 Native Host 注册脚本已创建。
- GitHub Actions Windows CI 已创建。
- Node.js 依赖已安装，npm audit 报告 0 个漏洞。
- `npm run check:extension` 已通过：TypeScript 类型检查成功，2 项协议测试通过。
- 扩展已构建到 `dist/edge-extension`。
- `docs/tooling.md` 已记录 Notion 官方导出、Pandoc 和 dws 的版本、许可证、维护状态、限制与退出方案。
- 已采用 Pandoc `3.8.2.1` 和钉钉官方开源 dws `1.0.59`，不自研 Markdown 解析、DOCX 生成或钉钉导入协议。
- 已创建包含中文路径、列表、表格、代码块和两张 PNG 的脱敏 Notion 导出夹具。
- 已提供 `scripts/convert-notion-export.ps1`，支持 Notion 导出目录或 ZIP，并兼容 Windows PowerShell 5.1。
- `npm run check:stage1` 已通过：目录和 ZIP 均成功生成自包含 DOCX；媒体数 2、图片关系数 2、外部图片关系数 0、Notion 临时 URL 数 0。
- dws OAuth 已在本机验证，按最小权限只授权 `doc` 文档业务域；凭据由 dws 与 Windows 当前用户存储管理。
- 指定专用目标文件夹后，DOCX 已成功转换为钉钉在线可编辑文档；返回了真实 `taskId` 与 `documentUrl`。
- 钉钉文档回读成功：标题、正文、嵌套列表、表格和两张图片均存在，图片资源属于钉钉 `alidocs2`，不含 Notion 地址。
- 已确认 dws `1.0.59` 的帮助与真实 API 有差异：当前导入必须显式提供 `folder` 或 `workspace`，不能依赖默认根目录。
- README 已调整为面向仓库使用者；详细计划由本文件维护。
- 仓库文档和开发记录采用“中文优先，专用名词保留官方写法”的语言约定。

尚未验证：

- 本地一条命令完成迁移的入口尚未实现。
- 目标文件夹选择、dws 登录预检、导入 JSON 解析和成功链接输出尚未统一封装。
- 缺失图片、损坏 ZIP、未登录、目标无权限和提交状态未知等失败路径尚未形成完整自动化测试。
- 钉钉图片的 24 小时持久性复查尚未完成。
- 多页面、数据库和 Notion 特殊块尚未完成系统覆盖。
- 当前开发机尚未安装 Go，已有 Go 代码没有完成本机构建与测试。
- Edge 扩展尚未在 `edge://extensions` 加载，Native Messaging 真实链路尚未打通；这些工作已后置到阶段 5。

下一步：

1. 新增一个本地迁移入口，把转换、目标参数、dws 导入和结果回读串成一条命令。
2. 将 `folder` 或 `workspace` 设为显式必填，避免 dws 默认根目录契约差异导致未知提交状态。
3. 增加 Pandoc、dws 版本与登录状态预检，并输出可操作的中文错误。
4. 解析 dws JSON 契约，成功时只输出文档链接与迁移摘要；未知状态时先回读或搜索，禁止自动重试写入。
5. 增加损坏 ZIP、缺失图片、未登录和无权限等阶段 2 验收测试。

## 进度维护规则

- 开始任务前先读取本文件的“当前进展”、当前阶段和验收条件。
- 当前优先保证本地工具可运行、可迁移、可验证；不要因为最终形态是 Edge 插件而提前开发 Edge 功能。
- 开发新能力前先检查官方工具和可靠开源项目；决定自研时必须记录现有方案未通过验收的证据。
- 只处理当前阶段，除非用户明确调整优先级或前置依赖要求提前实现下一阶段内容。
- 一个阶段只有在所有验收项实际通过后才能标记为“已完成”。
- 每次完成实质开发后更新“最后更新”“已完成”“尚未验证”和“下一步”。
- README 只同步用户可见能力、安装方式和公开限制，不复制本文件的详细内部计划。
- 如果设计决策改变产品契约、阶段顺序、工具选型或验收标准，先更新本文件，再实现代码。
