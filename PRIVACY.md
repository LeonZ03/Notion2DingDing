# Notion2DingDing 隐私说明

最后更新：2026-08-28

Notion2DingDing 是一个独立的本地迁移工具，用于把用户主动选择的 Notion 官方 HTML 导出包转换为钉钉在线文档。本项目与 Notion、钉钉和 Microsoft Edge 官方无隶属、联合或背书关系。

## 处理哪些数据

当用户打开扩展时，扩展会读取当前活动标签页的地址、页面标题以及当前可见块和图片的数量，用来识别是否为 Notion 页面并提供完整性提示。扩展不会把当前页面 DOM 当作迁移正文。

用户主动选择 Notion HTML ZIP 后，扩展会把该 ZIP 通过 Edge Native Messaging 传给当前 Windows 用户安装的本地助手。本地助手会处理导出包中的页面正文、标题、图片、链接和结构信息，并按用户选择的位置写入钉钉。

为恢复任务和避免无提示重复写入，工具可能在本机保存最小状态，包括任务 ID、源内容哈希、目标类型、状态、远端任务 ID、最终钉钉文档 URL、时间和清理结果。最小状态不保存正文、图片副本、原文件名、绝对输入路径、DOCX 路径或钉钉登录凭据。

## 数据如何使用和传输

- Notion 导出包的解析和中间格式转换在用户本机完成。
- 只有用户点击“开始转换”后，转换结果才会通过用户已授权的钉钉官方 dws 工具传给钉钉，以创建用户要求的文档。
- 项目不运营用于接收 Notion 文档、钉钉文档或迁移内容的中转服务器。
- 项目不包含广告、用户行为分析、跨站跟踪或遥测。
- 用户点击“下载本地助手”或“查看版本说明”时，Edge 会打开本项目的 GitHub Release 页面。GitHub、Microsoft Edge、Notion 和钉钉各自的数据处理受其服务条款和隐私政策约束。

## 凭据

扩展不读取或保存钉钉 Token、Cookie、AppKey 或 AppSecret。钉钉登录由官方 dws 完成，凭据由 dws 和 Windows 当前用户的凭据保护机制管理。Native Messaging 只允许清单中列出的 Notion2DingDing 扩展 ID 连接本地助手。

## 临时数据和删除

导出包副本、解压目录、图片副本、压缩图、DOCX 和临时日志属于内容型临时数据。迁移成功、明确失败或写入状态未知时，本地助手都会永久删除其创建的这些副本并验证路径不存在，不把它们移动到回收站。用户最初选择的 Notion ZIP 不会被自动删除。

用户可以卸载 Edge 扩展，并使用发布包提供的卸载入口删除本地助手、Native Host、配置和最小任务状态。卸载不会删除用户原始 Notion 导出包或已经创建的钉钉文档。

## 用户控制

所有迁移均由用户主动选择 ZIP 并再次点击“开始转换”触发。用户可以在写入钉钉前取消任务，可以更改钉钉登录和保存位置，也可以选择是否再次导出相同内容。对于可能已经写入但回执未知的任务，界面会提供已知的钉钉链接供用户核对。

## 联系与变更

问题、隐私请求或数据删除问题可通过项目 GitHub Issues 提交：<https://github.com/LeonZ03/Notion2DingDing/issues>。

功能或数据处理方式发生变化时，本说明会同步更新，并在需要时重新进行 Edge Add-ons 隐私披露。
